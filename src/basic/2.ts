// How can I rewrite it in TypeScript using the concept of tuples to ensure that 
// the first element is always a string and the second is a number?

// let person = ['Max', 21];

let person: [string, number];
person = ["Max", 21]

